#!/usr/bin/env bash
#Download latest version of opencv from
#Download ant (sudo apt install ant)
#Download python >=2.6
#Download java >7
#unzip opencv and cd to it
#mkdir build
#cd build
#export JAVA_HOME=***
#cmake -DBUILD_SHARED_LIBS=OFF ..
#make -j8

#being in build/ cp bin/opncv-xxx.jar to projectRoot/libs/
#if version other thatn 331, then update build.gradle accordingly

#make libopencv-java***.so visible with passing jvm argument -Djava.library.path=<opencvroot>/build/lib
